
#include <iostream>

namespace core
{

    /// @brief Main entry point for the core module.
    /// This function initializes the core module and runs the main application loop.
    /// This is primarily used for testing and development purposes.
    /// @return Exit code of the application.
    int main(int argc, char **argv)
    {
        // Initialize the core module
        std::cout << "Core module initialized." << std::endl;

        return 0; // Return success
    }

} // namespace core